// Global scope
var globalVar = "Im global variable";
let globalLet = "Im global variable, but scoped with let";
const globalConst = "Im global variable, but scoped with const";

// Block scope
{
    var blockVar = "Im block variable";
    let blockLet = "Im block variable, but scoped with let";
    const blockConst = "Im block variable, but scoped with const";
}
